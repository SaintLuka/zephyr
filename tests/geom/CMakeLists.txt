set(CRP_INTERFACE   1)
set(GAME_OF_LIFE    1)
set(MAPS_INTEGRAL   1)
set(MESH_GENERATOR  1)
set(MOVABLE_MESH    1)
set(POLYGON_CLIP    1)
set(VOLUME_FRACTION 1)

if (CRP_INTERFACE)
    add_executable(crp-interface crp_interface.cpp)
    target_include_directories(crp-interface PUBLIC zephyr)
    target_link_libraries(crp-interface PUBLIC zephyr)
    install(TARGETS crp-interface DESTINATION tests/geom)
endif()

if (GAME_OF_LIFE)
    add_executable(game-of-life game_of_life.cpp)
    target_include_directories(game-of-life PUBLIC zephyr)
    target_link_libraries(game-of-life PUBLIC zephyr)
    install(TARGETS game-of-life DESTINATION tests/geom)
endif()

if (MAPS_INTEGRAL)
    add_executable(maps-integral maps_integral.cpp)
    target_include_directories(maps-integral PUBLIC zephyr)
    target_link_libraries(maps-integral PUBLIC zephyr)
    install(TARGETS maps-integral DESTINATION tests/geom)
endif()

if (MESH_GENERATOR)
    add_executable(mesh-generator mesh_generator.cpp)
    target_include_directories(mesh-generator PUBLIC zephyr)
    target_link_libraries(mesh-generator PUBLIC zephyr)
    install(TARGETS mesh-generator DESTINATION tests/geom)
endif()

if (MOVABLE_MESH)
    add_executable(movable-mesh movable_mesh.cpp)
    target_include_directories(movable-mesh PUBLIC zephyr)
    target_link_libraries(movable-mesh PUBLIC zephyr)
    install(TARGETS movable-mesh DESTINATION tests/geom)
endif()

if (POLYGON_CLIP)
    add_executable(polygon-clip polygon_clip.cpp)
    target_include_directories(polygon-clip PUBLIC zephyr)
    target_link_libraries(polygon-clip PUBLIC zephyr)
    install(TARGETS polygon-clip DESTINATION tests/geom)
endif()

if (VOLUME_FRACTION)
    add_executable(volume-fraction volume_fraction.cpp)
    target_include_directories(volume-fraction PUBLIC zephyr)
    target_link_libraries(volume-fraction PUBLIC zephyr)
    install(TARGETS volume-fraction DESTINATION tests/geom)
endif()